<% if show_pagination? and @response.total_pages > 1 %>
 <div class="row record-padding">
  <div class="col-md-12">
    <div class="pagination">
      <%= paginate @response, :outer_window => 1, :window => 2, :theme => 'blacklight', :param_name => :page %>
    </div>
  </div>
 </div>
<% end %>

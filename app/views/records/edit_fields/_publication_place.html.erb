<%= f.input key,
            as: :controlled_vocabulary,
            placeholder: 'Search for a Publication Place using a GEONAMES label',
            input_html: {
              class: 'form-control',
              data: { 'autocomplete-url' => "/authorities/search/extended_geonames",
                      'autocomplete' => key,
                      'field-type' => 'linked_data' }
            },
            ### Required for the ControlledVocabulary javascript:
            wrapper_html: { data: { 'autocomplete-url' => "/authorities/search/extended_geonames",
                                    'field-name' => key,
                                    'field-type' => 'linked_data' }},
            required: f.object.required?(key) %>


<%= f.input key,
            as: :fetched_controlled_vocabulary,
            placeholder: 'Search for a Work Type using a valid URI',
            input_html: {
              class: 'form-control',
              data: { 'autocomplete-url' => "/authorities/search/work_type",
                      'autocomplete' => key,
                      'field-type' => 'linked_data' }
            },
            ### Required for the ControlledVocabulary javascript:
            wrapper_html: { data: { 'autocomplete-url' => "/authorities/search/work_type",
                                    'field-name' => key,
                                    'field-type' => 'linked_data' }},
            required: f.object.required?(key) %>

<%= f.input key,
            as: :controlled_vocabulary,
            placeholder: 'Search for a Repository using a valid URI',
            input_html: {
              data: { 'autocomplete-url' => "/authorities/search/repository",
                      'autocomplete' => key,
                      'field-type' => 'linked_data' }
            },
            ### Required for the ControlledVocabulary javascript:
            wrapper_html: { data: { 'autocomplete-url' => "/authorities/search/repository",
                                    'field-name' => key,
                                    'field-type' => 'linked_data' }},
            required: f.object.required?(key) %>


<%= f.input key,
            as: :fetched_controlled_vocabulary,
            placeholder: 'Search for a Local Collection Name using a valid URI',
            input_html: {
              class: 'form-control',
              data: { 'autocomplete-url' => "/authorities/search/local_collection_name",
                      'autocomplete' => key,
                      'field-type' => 'linked_data' }
            },
            ### Required for the ControlledVocabulary javascript:
            wrapper_html: { data: { 'autocomplete-url' => "/authorities/search/local_collection_name",
                                    'field-name' => key,
                                    'field-type' => 'linked_data' }},
            required: f.object.required?(key) %>

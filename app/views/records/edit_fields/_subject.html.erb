<%= f.input key,
            as: :label_and_uri_controlled_vocabulary,
            placeholder: 'Search for a Subject using a valid URI',
            input_html: {
              class: 'form-control',
              data: { 'autocomplete-url' => "/authorities/search/subject",
                      'autocomplete' => key,
                      'field-type' => 'linked_data' }
            },
            ### Required for the ControlledVocabulary javascript:
            wrapper_html: { data: { 'autocomplete-url' => "/authorities/search/subject",
                                    'field-name' => key,
                                    'field-type' => 'linked_data' }},
            required: f.object.required?(key) %>

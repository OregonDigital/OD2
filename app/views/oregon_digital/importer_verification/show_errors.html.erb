<div class="row">
  <div class="col-sm-6">
    <h3>Errors for <%= @importer.name %> </h3>
    <% @errors.keys.each do |key| %>
      <p>Work id: <%= key %><ul>
        <% @errors[key].each do |err| %>
          <li><%= err %></li>
        <% end %>
      </ul></p>
    <% end %>
  </div>
</div>

<div class="container-fluid">
  <div class="row">
    <div class="col-12 col-md-4">
      <%= media_display @presenter %>
      <%= render 'show_actions', presenter: @presenter %>
      <%= render 'single_use_links', presenter: @presenter if @presenter.editor? %>
    </div>
    <div itemscope itemtype="<%= @presenter.itemtype %>" class="col-12 col-md-8">
      <header>
        <%= render 'file_set_title', presenter: @presenter %>
      </header>

      <%# TODO: render 'show_descriptions' See https://github.com/samvera/hyrax/issues/1481 %>
      <%= render 'show_details' %>
      <%# OVERRIDE FROM HYRAX TO: only display user activity log to admins %>
      <%= render 'hyrax/users/activity_log', events: @presenter.events if !current_user.nil? && current_user.admin? %>
    </div><!-- /columns second -->
  </div> <!-- /.row -->
</div><!-- /.container-fluid -->

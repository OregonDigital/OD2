<div id="search">
  <%= render 'search_form' %>
  <h1><%= application_name %> <%= t('.users') %></h1>
</div>
<table class="table table-striped">
    <thead>
        <tr>
            <th><%= t('.avatar') %></th>
            <th class="sorts"><i id="name" class="fa <%= params[:sort].blank? ? 'fa-caret-up' : params[:sort] == "name desc" ? 'fa-caret-down' : params[:sort] == "name" ? 'fa-caret-up' : '' %>"></i> <%= t('.user_name') %></th>
            <th class="sorts"><i id="login" class="fa <%= params[:sort] == "login desc" ? 'fa-caret-down' : params[:sort] == "login" ? 'fa-caret-up' : '' %>"></i> <%= t('.user_id') %></th>
            <th class="sorts"><i id="department" class="fa <%= params[:sort] == "department desc" ? 'fa-caret-down' : params[:sort] == "department" ? 'fa-caret-up' : '' %>"></i> <%= t('.department') %></th>
            <th><%= t('.works_created') %></th>
            <th><%= t('.activated') %>
        </tr>
    </thead>
    <tbody>
    <%= render partial: 'user_row', collection: @users, as: :user %>
    </tbody>
</table>
<div class="pager">
  <%= paginate @users, theme: 'blacklight', route_set: hyrax %>
</div>

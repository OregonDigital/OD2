require 'linkeddata' # we need all the linked data types, because we don't know what types a service might return.
module OregonDigital
  class OdDeepIndexingService < Hyrax::DeepIndexingService
  end
end